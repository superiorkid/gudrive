services:
    db:
        image: postgres:17-alpine
        volumes:
            - ./var/db:/var/lib/postgresql/data
        ports:
            - "5433:5432"
        environment:
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=postgres
            - POSTGRES_DB=kandangdata
